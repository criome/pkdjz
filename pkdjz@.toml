[atom]
id = "pkdjz"
version = "0.1.0"
system = true
propagate = true

[std]
features = ["lib"]

[backend.nix]
fetcher = "flake-lock"

[fetch.nixpkgs-atom]
name = "nixpkgs-atom"

[fetch.omnix]
name = "omnix"
type = "flake"

[fetch.zed]
name = "zed"
type = "flake"
